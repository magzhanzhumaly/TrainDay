# TrainDay 🏃‍♂️📅

TrainDay — iOS-приложение на SwiftUI для отображения календаря тренировок с просмотром списка тренировок по дням и детальной информацией о каждой тренировке.

Проект выполнен как тестовое задание с акцентом на:
- чистую архитектуру
- работу с датами и календарём
- MVVM + Coordinator
- работу с моковыми данными, имитирующими сервер

---

## 🧩 Основной функционал

### Календарь
- Отображение календаря текущего месяца
- Навигация между месяцами (вперёд / назад)
- Сегодняшний день визуально выделен
- Дни с тренировками помечены цветными индикаторами
- При открытии текущего месяца по умолчанию выбран **сегодняшний день**
- При открытии любого другого месяца выбран **первый день месяца**

### Список тренировок
- Отображение тренировок выбранного дня
- Для каждой тренировки показывается:
  - тип тренировки
  - время начала

### Детали тренировки
- Тип тренировки
- Дата и время
- Дистанция
- Длительность
- Комментарий (если есть)
- График пульса (если данные присутствуют)
- Корректная обработка отсутствующих данных

---

## 🧱 Архитектура

Проект построен на **MVVM + Coordinator**.

### Структура проекта

TrainDay/
├── App/                 # Точка входа и root view
├── Coordinators/        # Навигация и маршруты
├── Models/              # Модели данных
├── Services/            # Работа с данными (mock API, JSON, парсинг)
├── ViewModels/          # Бизнес-логика экранов
├── Views/               # SwiftUI экраны и компоненты
├── Resources/
│   └── test_data/       # Моковые JSON-данные
└── Tests/               # Unit-тесты логики

### Ключевые решения
- `CalendarViewModel` изолирован через `@MainActor`
- Навигация полностью вынесена в `AppCoordinator`
- Работа с датами централизована через `DateParser`
- Моковые данные загружаются через `BundleWorkoutAPI`
- JSON-структуры повторяют формат реального API

---

## 🗃️ Данные (Mock API)

Данные хранятся локально в `Resources/test_data`:

- `list_workouts.json` — список тренировок
- `metadata.json` — метаданные тренировок
- `diagram_data.json` — данные для графиков

Формат данных соответствует описанию API в задании и имитирует работу сервера.

---

## 🧪 Тесты

Проект содержит unit-тесты, написанные с использованием **Swift Testing** (`import Testing`):

Покрыто:
- Логика `DayKey` (сравнение дат)
- Парсинг дат через `DateParser`
- Дефолтный выбор даты в календаре
- Корректное определение первого дня месяца
- Фильтрация тренировок по дню

Файл тестов:

TrainDayTests/TrainDayTests.swift

---

## ⚙️ Технические требования

- **Язык:** Swift
- **UI:** SwiftUI
- **Архитектура:** MVVM + Coordinator
- **Минимальная iOS:** iOS 16+
- **Без сторонних UI-фреймворков**
- **Данные:** локальные JSON (mock server)

---

## ▶️ Запуск проекта

1. Открыть `TrainDay.xcodeproj`
2. Выбрать iPhone симулятор или реальное устройство
3. Запустить проект (`Cmd + R`)

---

## ✨ Дополнительно

Проект ориентирован на расширение:
- легко заменить `BundleWorkoutAPI` на реальный сетевой слой
- можно добавить новые типы тренировок
- можно расширить аналитику и графики

---

## 👤 Автор

Magzhan Zhumaly
iOS Engineer

